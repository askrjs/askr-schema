{
  "name": "@askrjs/schema",
  "version": "0.0.9",
  "description": "Executable schemas with deterministic OpenAPI projection for Askr",
  "keywords": [
    "askr",
    "openapi",
    "schema",
    "validation"
  ],
  "homepage": "https://github.com/askrjs/askr-schema#readme",
  "bugs": {
    "url": "https://github.com/askrjs/askr-schema/issues"
  },
  "license": "Apache-2.0",
  "author": {
    "name": "askrjs",
    "url": "https://github.com/askrjs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/askrjs/askr-schema.git"
  },
  "files": [
    "dist/**/*.js",
    "dist/**/*.d.ts"
  ],
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "vp pack",
    "lint": "vp lint src && npm run typecheck",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vp test run",
    "test:types": "npm run build && tsc -p tests/types/tsconfig.json",
    "test:packed": "node tests/packed-scalar-contract.mjs",
    "test:publint": "publint",
    "pack:check": "npm pack --ignore-scripts --dry-run --json",
    "check": "npm run lint && npm run typecheck && npm test && npm run test:types && npm run build && npm run test:packed && npm run test:publint && npm run pack:check",
    "prepack": "npm run build",
    "prepublishOnly": "npm run check"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "ajv": "^8.20.0",
    "publint": "^0.3.23",
    "typescript": "^7.0.2",
    "vite-plus": "^0.2.8",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "packageManager": "npm@12.0.1"
}
